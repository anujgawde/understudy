{
  "name": "@understudy/target-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Target App — a deliberately hostile legacy banking application for the agent to operate against.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
