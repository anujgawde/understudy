# Makes the root package.json `engines` field a hard failure rather than a
# warning. Node 22 cannot require() an ES module inside a circular dependency
# chain, which breaks `nest build` in a way that reads as a source bug.
engine-strict=true
